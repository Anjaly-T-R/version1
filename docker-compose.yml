
services:
  api:
    build: .
    env_file: .env
    ports:
      - "80:4000"
    volumes:
      - ./data:/app/data
    environment:
      DB_HOST: database-1-instance-1.ce2haupt2cta.ap-southeast-2.rds.amazonaws.com
      DB_PORT: 5432
      DB_USER: s408
      DB_PASSWORD: FPzoBUiC7eTX
      DB_NAME: cohort_2025
